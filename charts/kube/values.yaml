replicaCount: 1

image:
  repository: containerum/kube
  tag: v1.0.0
  pullPolicy: IfNotPresent
  secret:

service:
  port: 1214
  targetPort: 1212
  externalIP:

rbac:
  serviceAccountName: null

env:
  DEBUG: "true"
  TEXTLOG: "true"
